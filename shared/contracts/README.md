# Shared Contracts

Use this folder for boundary contracts shared between modules.

Keep out:
- provider-specific adapters
- transport-specific implementation details
- internal-only module APIs

Deferred in this slice:
- concrete request and response contracts
- validation schemas
- transport bindings
