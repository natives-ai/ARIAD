# Shared Types

Use this folder for data shapes that must be consumed across module boundaries.

Keep out:
- frontend-only view state
- backend-only storage internals
- recommendation-only internal implementation details

Deferred in this slice:
- language-specific type files
- concrete shared type definitions
