{
  "name": "@ariad/recommendation",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "yarn run -T tsc -p tsconfig.json",
    "typecheck": "yarn run -T tsc -p tsconfig.json --noEmit",
    "lint": "yarn run -T eslint src --max-warnings=0",
    "test": "yarn run -T vitest run --exclude dist/**"
  }
}
